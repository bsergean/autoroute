version: "3.3"
services:
  push:
    entrypoint: ws push_server --host 0.0.0.0
    image: bsergean/ws:10.5.7

  autoroute:
    entrypoint: /usr/bin/autoroute
    image: ${DOCKER_REPO}/autoroute-swift:build
    depends_on:
      - push
